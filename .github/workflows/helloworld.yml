name: Hell world demo

on:
  push:
    branches:
      - dev

jobs: 
  # Print hello world
  print:
    runs-on: ubuntu-latest
    steps:
      - name: print text
        run: |
          echo "Hello world..!",
          echo "This is sample workflow file for dev branch"